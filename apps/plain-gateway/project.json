{
  "root": "apps/plain-gateway",
  "sourceRoot": "apps/plain-gateway/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/plain-gateway",
        "main": "apps/plain-gateway/src/main.ts",
        "tsConfig": "apps/plain-gateway/tsconfig.app.json",
        "assets": ["apps/plain-gateway/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/plain-gateway/src/environments/environment.ts",
              "with": "apps/plain-gateway/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "plain-gateway:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/plain-gateway/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/plain-gateway"],
      "options": {
        "jestConfig": "apps/plain-gateway/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
